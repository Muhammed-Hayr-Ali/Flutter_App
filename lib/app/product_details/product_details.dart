import 'package:application/packages.dart';

class ProductDetailsOld extends StatelessWidget {
  const ProductDetailsOld({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(
      child: Text('ProductDetailsOld'),
    ));
  }
}
